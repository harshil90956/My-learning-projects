<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Chats</title>
</head>
<body>
    <h3>Edit your chat : <%=chat._id%></h3>
    <p>This chat send from <b><%= chat.from %></b> to <b><%= chat.to%></b></p>
    
    <form method="post" action="/chats/<%= chat._id%>?_method=PUT">
        <textarea name="msg" cols="35" rows="5"><%= chat.msg%></textarea>
        <br>
        <button>Edit</button>
    </form>

</body>
</html>